format: json
exit-code: 1
debug: false
severity:
    - CRITICAL
    - HIGH
scan:
    scanners:
        - vuln
        - secret
vulnerability:
    ignore-unfixed: true
pkg:
    # Same as '--include-dev-deps'
    include-dev-deps: true
