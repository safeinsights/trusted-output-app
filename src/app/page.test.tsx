import { describe, it } from 'vitest'

describe('Main Page', () => {
    it('redirects when not signed in', async () => {
        // TODO Write test for this at some point
        //  Instead of redirecting, we use basic auth which just
        //  has the built in popup
    })
})
